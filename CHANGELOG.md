# 更新日志 CHANGELOG

本项目所有显著变更均记录于此。版本号采用语义化版本，标注日期为发布日（UTC+8）。

## [3.0-lite] - 2026-09-21

### 极致瘦身至约 80KB：端侧极简 + 云端按需分片
- **体积**：APK 从 v2.9 的约 1.5MB 降到 **约 80KB（<100KB）**。
- **端侧只留**：一/二/三级简码（全留，五笔快速输入核心）+ 全码 top1500 常用单字。
- **云端分片**：全部词组（二~十六字）与其余单字按"编码前 2 码"切成 650 个 JSON（约 2.9MB），托管于 GitHub 仓库，经 jsDelivr CDN（`cdn.jsdelivr.net/gh/zsdili/CloudWubi-Glass-Keyboard@main/cloud/`）按需 fetch、内存缓存；满 4 码异步拉取，端侧候选先即时显示、云端回来再刷新，不阻塞输入。
- **离线降级**：无网络时使用端侧简码 + 常用单字，键盘基础输入不中断（离线无法出云端词组）。
- **视觉（液态玻璃透光/反光/阴影）**：键盘底板加磨砂底色（淡青蓝→淡紫透光渐变 + 强 blur，盖住宿主 App 文字、不干扰识别）；按键强化水晶凸起——顶部镜面反光带（::after）、左上亮棱、右下内暗、底部柔投影；深/浅色底板统一提高不透明度。
- **底排标点键改为上滑**：空格键左侧点击上屏"，"、上滑上屏"！"；右侧点击上屏"。"、上滑上屏"？"（中英文随状态，固定不再切换默认）。
- **红线再加固**：补过滤"起义/党委/军委/纪念碑/特务/汉奸/卖国/清真寺"等漏网词干；全分片复扫零政治/宗教/军事残留。
- 可读源码移至 `web-src/`（不进 APK）；构建链：`gen_wubi_data → gen_cloud（云端分片）→ gen_lite（端侧）→ minify_assets → assembleRelease(R8)`。

## [2.9-lite] - 2026-09-21

### 多源融合 + 独立对照审计（用魔法打败魔法）
- **根因（S）定位**：top3000 常用词命中 96.2%，打不出的词中绝大多数是政治/政府/军事/战争/宗教/制度词（红线正确过滤）；真正的覆盖缺口来自 ① jieba 是"分词词典"，不收录口语/语气/句子（不会吧、好吧、吃饭了吗、去哪了）；② 第三方大表 RAW 假阳性因子（人民/社会/公司）被子串匹配误伤，且"由高频成分构成、整体无词频"的组合词（人民群众）无法靠高频保护幸免。
- **词组多源融合**：在 jieba 高频词（约9.4万）之外，新增成语（chinese-xinhua，约3.0万，含长谚语）、歇后语谜底（约1.6万）、手工精选日常口语/俗语（约369条，X吧/X吗/X呢/应答/祝福）；四字词从约1万增至3.75万。
- **RAW 假阳性因子自动审计（FACTOR_ALLOW）**：用 jieba 真实词频反向审计第三方大表——大表中"其本身是高频正常词(≥100)、且不在手工红线"的因子自动判为假阳性、不生效。
- **第三方大表改为整词精确匹配**：只删"词本身就是敏感词"，不做子串连带，消除"论功行赏/众口交赞/春色满园/迅雷不及掩耳"等正常成语被误伤。
- **独立对照审计器 `audit.js`（第二大脑）**：直接审端侧产物、独立取码与判定，做三项交叉核对——正向覆盖（该打的能否打出）、反向违规（不该的是否漏网）、情境证据（无任何语料佐证即乱造）；审计→修复→再审计。
- 放宽多字词长度到 16 字（容纳经典长谚语，取码逻辑不变）。
- 验证（离线代码级，独立审计裁决 PASS）：正向覆盖 98.3%、零缺口；反向零违规；情境零乱造；gisv「不要」、wnvw「人民群众」、wwna「人民」、gwkc「不会吧」、kqbk「吃饭了吗」、fkbn「去哪了」、lyab「国庆节」首选全部正确。

## [2.8-lite] - 2026-09-21

### 词库净化 · 白名单闭环（六条边界 + 话题红线）
- **词组来源重构**：词组不再直接沿用第三方词库，改为从规范现代汉语词频白名单（jieba，词频≥10 纯中文，约 9.6 万）离线按五笔取码规则生成，零生造。
- **物理封禁运行时动态造词**：移除满 4 码笛卡尔积实时拼词——词库已由完整规范白名单生成，运行时组合只会产生词库外杂质；"打不出"靠更全白名单解决。
- **话题红线（约 337 词，所有词频都过滤、不豁免）**：不涉及国家/政策/政治/宗教/战争/民族/民俗，以及黄赌毒、脏话、极端消极词。
- **高频保护**：第三方违禁大表假阳性较高（误伤"手机/公司/电话"等），对词频≥500 的高频常用词豁免该大表；真正违禁词不会有高词频。
- **用户私有白名单**：用户主动上屏、词库未收且过安全预检的多字词才学习，精确匹配、不连锁、上限 1000、设置中可一键清空，仅存本机。
- 单字/一~四级简码完全保留五笔86原方案（25 个一级简码置顶验证通过）。
- 新增设置「数据管理」：清空自定义词 / 清空最近用字。
- 验证（离线代码级）：25 一级简码全部置顶；gisv 零杂质且「不要」居首；话题词全部剔除、高频正常词全部保留；用户词学习闭环通过。

## [2.7-lite] - 2026-09-21

### 核心引擎（减少词库依赖：动态造词 CDD）
- 新增「动态造词（Code Dynamic Derivation）」：满 4 码按五笔取码规则，用单字全码前缀实时拼出词组，不依赖词组库——二字词 2+2、三字词 1+1+2、四字词 1+1+1+1。
- 动态二字词按单字频率估计词频，与静态二字词统一排序：词库漏收的高频搭配（如 gisv「不要」，原词库误收「还要」）也能拼出并置顶，空格直接上屏。
- 三、四字词动态兜底，覆盖新词 / 人名 / 临时组合。
- 说明：五笔词组恒为 4 码，五字以上长词 4 码信息不足、无法无歧义动态拼出，仍由词库（端侧常用 + 云端长词库）承担。

### 词库
- 多源合并：rime/rime-wubi 官方（真实词频、排序准）+ 极点 jidian（补充），共约 7.6 万条；按字数归一化真实词频。

### 视觉 / 交互
- 默认主题回归浅色水润清透玻璃（淡青蓝→浅紫灰）。
- 工具栏文字不加粗。

## [2.6-lite] - 2026-09-21

### 视觉（深色玻璃质感重做：从哑光灰到通透水晶）
- 修正 v2.5 深色玻璃「哑光、平贴、像塑料」的问题：按键改为凸起水晶——顶部强镜面反光带、边缘亮描边、中部通透透出 App、底部投影，光影方向统一（光源左上）。
- 真机硬件 GPU：底板深色磨砂 + backdrop 模糊、明显透出后方内容；功能键同材质、明暗稍区分，回车蓝色水晶。
- 软件 GPU（Redroid / SwiftShader）：自动改不透明近似（保留顶部亮白反光带），不受低透明光栅化限制；真机呈现不受影响。
- 全局禁用点击后的 focus 描边（去除部分机型黄绿焦点框）。

## [2.5-lite] - 2026-09-21

### 视觉（默认主题改为深色磨砂液态玻璃）
- **默认主题切换为 iOS 控制中心同款深色磨砂玻璃**：中性深灰半透明磨砂底板（顶部稍亮、底部渐暗），按键为深灰玻璃切割，白色文字 / 图标，蓝色回车作为彩色点缀，整体细腻通透。
- 主键（字母 / 数字）中性深灰玻璃；功能键（Shift / 退格 / 运算符 / 标点）同材质、明暗稍作区分；候选栏、算式区统一深灰玻璃底板（不再纯白）。
- 覆盖安装自动迁移到深色（一次性）；设置中仍可切回「清透玻璃（浅色）」或「幻紫霓虹」。
- 深色玻璃各段不透明度保持在软件 GPU 可光栅化阈值以上，Redroid（SwiftShader）与真机呈现一致。

## [2.4-lite] - 2026-09-21

### 交互 / 功能（真机反馈修复）
- **Shift 升级三态**：智能大小写（句首大写、句中小写，默认）→ 大写锁定 Caps（全部大写）→ 小写；点 Shift 循环切换，键面与上屏实时一致。
- **字母键上滑标点随中英文模式切换**：
  - 中文：第一行《》“”…—【】「」，第二行 ~@#$%&*()，第三行 '/-_:;、（M=顿号）；
  - 英文：第一行 <>{}[]"^\|，第二行 ~@#$%&*()，第三行 '/-_:;.（M=小数点）。
- **底排「，！」「。？」改上滑切换**（点击上屏当前标点、上滑切换主副），副标点改半透明白适配玻璃底。
- **数字盘**：小数点与空格对换（小数点在 9 右、空格在 0 右）；工具栏分段「123」标签改为「算」。
- **计算器续算固化**：在已能算出结果的算式后按运算符续算，自动以结果值起算，公式呈现「18−10=8」而非「9×2−10=8」，链式如「18×3=54」。
- 主键（白玻璃）与功能键（蓝紫玻璃）色彩对比加强，一眼可辨。

### 触感
- 按键震动调为清脆可感知（25ms / 幅度 200），仍可在设置关闭。

## [2.3-lite] - 2026-09-20

### 视觉（按图一重做玻璃透光质感）
- **整块键盘回归「一块完整玻璃板切割」**：底板淡蓝（左上）→ 淡紫（右下）柔和渐变；所有按键为透明磨砂水晶，光源恒在左上角——左上亮白高光，右下边缘透出淡蓝→淡紫渐变，底部同色悬浮投影。
- 主键（字母 / 数字）最通透、深灰细体字（文字带轻微白光泽）；功能键（Shift / 退格 / 运算符 / 123 / 标点）同材质、淡蓝紫玻璃稍深一档、白色细线条；回车 / 确认再深半档，仍透明磨砂、光影一致。
- 候选栏、算式区改玻璃底板（不再纯白）；顶部功能键更通透偏白。
- 杜绝纯色填充与塑料感，按键与背景配色完全统一。

### 软件 GPU 适配
- 修正低透明（alpha < .46）渐变在 SwiftShader 下不光栅化、导致按键整组透明：统一玻璃渐变各段不透明度阈值，真机与模拟器呈现一致。

## [2.2-lite] - 2026-09-20

### 视觉（按图6 回归浅色清透玻璃）
- **主题改为图6「浅色清透玻璃」**：淡蓝→淡紫柔和渐变、高透玻璃卡片，细亮描边、顶部高光 / 底部柔影、凸起圆键；文字深蓝灰，整体清透干净，**不沿用任何旧版本配色**。深色 / 霓虹主题保留为可选项。
- 功能键淡蓝紫彩色玻璃白字，回车 / 确认清透蓝紫渐变。

### 语音
- **重构语音引擎选择**：中文环境按 vivo Jovi(95) > 讯飞(92) > 百度(88) > 国内厂商(70) > Google(45) 打分排序；支持在设置中手动指定引擎，当前引擎失败自动切换下一个并提示重说，全部耗尽才报错。
- 新增引擎列表 / 读取 / 指定桥方法，诊断输出引擎评分与手动选择。

### 布局与入口
- **底排改为 5 键**：123 / ！，（感叹号·逗号切换）/ 空格（点击空格、长按语音）/ ？。（问号·句号切换）/ 回车；符号入口统一收归工具栏。
- **数字盘布局固化**：1 在左上角、0 在 8 正下方、0 左为百分号、0 右为小数点；空格移至 9 的右侧（单击上屏默认备选，优先计算纯结果）。
- 工具栏重排为：贴 / 123｜符 …… 表情 / 译 / 智 / 设置 / 键盘 / 收起；表情移到「译」前，工具栏按钮全局统一适配。
- 主键盘按键高度再提高约 1/3。

### 软件 GPU 适配（回归环境）
- **自动检测软件 GPU（SwiftShader / 模拟器）**：软件 GPU 下自动关闭背景模糊、符号 / 表情分页由横向滚动改为页切换，并在面板切换后强制重绘，解决内容透明；真机硬件 GPU 保持玻璃模糊与手拖动平滑。

## [2.1-lite] - 2026-09-20

### 词库
- **新增高频口语 / 问候 / 歇后语自定义词库** `tools/custom_phrases.txt`（约 330 条、295 条入库），按五笔词组规则生成编码；晚上好、去哪里、吃了饭吗、怎么办及多条歇后语可直接打出。
- **修复规范字“么”被 opencc 误转为“幺”而错剔**：改为《通用规范汉字表》白名单优先，白名单外才用繁简转换判繁体。

### 视觉（按真机反馈回归鲜艳果冻）
- **换回 iOS27 鲜艳果冻液态玻璃**：半透明深色基底 + 高饱和多彩流体（粉 / 橙 / 绿 / 青 / 紫），果冻白键顶部强高光、底部内阴影、Q 弹回弹；功能键紫蓝彩色玻璃，回车 / 确认蓝青紫渐变（修正上一版偏暗、偏灰、"颜色太丑"）。
- 实时背景模糊保持 14px、饱和度 150%，在软件 GPU 下合成稳定、真机流畅省电。

### 布局与入口
- **底排标点改为上滑上屏**：字母键上标小号标点（颜色更浅、居中于字母上方），上滑输入；底排由 5 键简化为 **123 / 符 / 空格（长按语音）/ 回车** 4 键，去除重复的标点切换。
- **数字 / 符号切换移至工具栏「剪贴板（贴）」右侧**，分段控件「数字｜符号」单一高亮、互相排他；符号面板不再保留重复切换条。
- **工具栏图标文字化**：剪贴板改「贴」、短语改「智」；表情仅保留唯一入口（左上角 😊），杜绝重复功能键。
- **上滑标点最终排布（逐字唯一、合中文习惯）**：Q 行 《 》 “ ” … — 【 】 「 」；A 行 ， 。 ？ ！ 、 ： ； （ ）；Z 行 ～ @ # ＄ ％ ＆ ·。
- **数字行上滑 = 点击上屏**（数字行不常驻角标，长按数字仍出对应常用标点）。

### 符号与中文直出
- **符号键盘每分类扩充至 40 个**（常用 / 中文 / 数学 / 特殊 / 英文，分页可手拖动平滑切换），右侧退格长条、右下确认，位置 / 大小 / 颜色统一。
- **中文词直接候选符号 / 表情**：打「加 / 减 / 乘 / 除 / 等于 / 括号 / 百分之 / 千分之 / 大于 / 小于 / 不等于」候选 ＋ － × ÷ ＝ （） ％ ‰ ＞ ＜ ≠；打「笑 / 哭 / 爱 / 花 / 星 / 火 / 水 / 太阳 / 月亮 / 赞」候选对应表情。

### 安全
- **密码框剪贴板隐私加固**：密码 / 数字密码框中剪贴板面不暴露任何历史（含此前已收集内容），仅提示"密码框已保护剪贴板"。

### 可靠性（软件 GPU 渲染，实测修正）
- **修正软件 GPU（Redroid / SwiftShader）下字母键首次透明**：实测「渐变背景 + 按键独立合成层 `translateZ(0)`」会导致首次光栅化透明；**移除按键静态 `translateZ(0)`**、在父层统一光栅化后首次即正常（与早期纯色背景下的结论相反，以本次真机实测为准）；按压 scale 由硬件 GPU 自动合成，手感不受影响。

### 验证
- Headless 核心回归 **279 项全绿**、功能回归 **80 项全绿**；Redroid Android 11（WebView Chrome 125）真机黑盒回归 **279 项全绿**，含一级简码 25、二 / 三 / 四级简码与词组各 50、用户词置顶、计算 50、链式计算；字母 / 数字 / 符号三盘截图留存。

## [2.0-lite] - 2026-09-20

### 布局与视觉
- **数字键盘改为电话盘布局**：1 在左上角（123 / 456 / 789），0 在 8 的正下方；左侧为跨三行的运算符长条（＋－×÷），右列为退格 / @ / 小数点，底行为返回 / 空格 / 0 / 符号 / 回车。
- **ASDF（第二字母行）左右各缩进半个键**，与主流手机键盘对齐。
- **按键加大、键间距减小**（间距 7px → 4px），降低误触。
- 字母键上滑标点**全局重排**：采用中文高频、互不重复的标点（括号、引号、书名号、顿号、省略号、间隔号、货币符号等），底排已有的 ，。？！不重复占用。
- 按键触点增加按压内发光反馈。

### 输入逻辑
- **修复词频调整未生效**：单字现在会记录到最近上屏，简码时高频锚字置顶、全码时按「最近 + 高频」排序；词组跨长度置顶规则保留。
- **空格选词 / 上屏编码时不再附带空格**。
- **修复按键震动不触发**：JS 端无参调用导致桥接方法未命中，改为传参并在原生侧补充无参重载，默认轻微震动 14ms。

### 性能
- 降低主背景实时模糊强度（22px → 14px）与饱和度，避免在软件渲染（无独立 GPU）环境下合成线程卡死，真机也更流畅省电。

### 验证
- Headless 回归：核心 279 项、功能 80 项全部通过。
- Redroid 容器化 Android（Android 11）真机注入回归：279 项全部通过、0 失败。

## [1.9-lite]
- 词库加厚：三 ~ 五字词进入端侧，修复多字词打不出的问题；长词整取不切碎。
- 重写候选排序：精确 / 前缀、z 键通配，四码词组优先。
- 数字盘「只自动算结果、不自动上屏」，公式与纯结果可分别选择。
- 用户词跨长度置顶（新鲜度优先）。
- 修复首次进入键盘高度为 0、不弹出的时序问题。
- 以《通用规范汉字表》8105 字为白名单，剔除生僻字与残留繁体（词库收敛至约 7.4 万条）。

## [1.8-lite]
- 密码框适配：文本 / 数字密码下不自动大写、不读剪贴板、不做语义泄露。
- 功能键液态玻璃化；数字盘与金融盘对齐。
- 双击空格出句号、顶部下滑收起、底部安全区适配、候选栏加高。

## [1.7-lite]
- 统一全局锚点（返回左上、确认右下、退格在确认上方）。
- 键面宽高比 3:4；字母键增加标点上标与上滑输入。
- Shift 二态；键盘消失再显示恢复默认状态。

## [1.6-lite]
- 数字盘 ABC 切换移到左侧；算式 / 结果按 5:1 展示。
- 修复全选文本后无法退格删除。

## [1.5-lite]
- 数字计算键盘；显式选择语音识别引擎；语音超时处理。

## [1.4-lite]
- 加固退格删除；键盘加大；顿号键改为 Shift；语音超时兜底。

## [1.3-lite]
- 前端三件套（HTML/CSS/JS）重写，确立点击委托 + 指针事件（按压 / 长按 / 连删 / 滑动）体系。

## [1.2-lite]
- 光标跟随上下文联想、整段翻译；底排功能改造。

## [1.1-lite]
- 引入 Rime 五笔 86 词库，使用 opencc 转简体并剔除繁体。

## [1.0-lite]
- 首个版本：手工五笔码表、基础字母键盘。
